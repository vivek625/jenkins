apiVersion: v1
kind: Service
  metadata:
    name: spring-boot-app-service
  spec:
    type: LoadBalancer
    ports:
    - name: http
      port: 80
      targetPort: 8083
      protocol: TCP
    selector:
      app: spring-boot-app
